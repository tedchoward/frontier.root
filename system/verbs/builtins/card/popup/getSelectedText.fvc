FrontierVcsFile:3:scpt:system.verbs.builtins.card.popup.getSelectedText

on getSelectedText (objectname) {
	local (atts);
	new (tabletype, @atts);
	if not card.getObjectAttributes (objectname, @atts) {
		return (-1)};
	return (atts.sels)}


