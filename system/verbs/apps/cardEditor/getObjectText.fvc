FrontierVcsFile:3:scpt:system.verbs.apps.cardEditor.getObjectText

on getObjectText (name) {
	local (atts);
	if not cardEditor.getObjectAttributes (name, @atts) {
		return ("")};
	return (atts.text)}


