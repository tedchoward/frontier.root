FrontierVcsFile:3:scpt:system.verbs.apps.cardEditor.getObjectSeeThru

on getObjectSeeThru (name) {
	local (atts);
	if not cardEditor.getObjectAttributes (name, @atts) {
		return ("")};
	return (atts.tran)}


