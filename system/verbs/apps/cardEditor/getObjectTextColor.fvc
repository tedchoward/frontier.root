FrontierVcsFile:3:scpt:system.verbs.apps.cardEditor.getObjectTextColor

on getObjectTextColor (name) {
	local (atts);
	if not cardEditor.getObjectAttributes (name, @atts) {
		return ("")};
	return (atts.txtc)}


