FrontierVcsFile:3:scpt:system.verbs.apps.cardEditor.getObjectFlag

on getObjectFlag (name) {
	local (atts);
	if not cardEditor.getObjectAttributes (name, @atts) {
		return ("")};
	return (atts.flag)}


