FrontierVcsFile:3:scpt:system.verbs.apps.Filemaker.examples.oldExamples.removeMenus

on removeMenuItems() {
	with objectModel, FileMaker {
		«I would prefer to count the menu items, but FileMaker prefers the following
		try {
			loop { « the try terminates the loop, when there is no menuItem[1]
				delete(menuItem[1]);
				rollBeachBall ()}}}};
removeMenuItems()


