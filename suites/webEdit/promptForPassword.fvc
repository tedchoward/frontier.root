FrontierVcsFile:3:scpt:suites.webEdit.promptForPassword

on promptForPassword (password, serverName) {
	if password != "" {
		return (password)};
	if not (dialog.ask ("Password for " + serverName + ":", @password)) {
		return (false)};
	return (password)}


