FrontierVcsFile:3:scpt:suites.FilemakerLib.hideWindow

on hideWindow (db, reportError = false) {
	on doIt () {
		return (filemaker.hideWindow (db))};
	if reportError {
		return (doIt ())}
	else {
		try {
			return (doIt ())}
		else {
			return (false)}}}


