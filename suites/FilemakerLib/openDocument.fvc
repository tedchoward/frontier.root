FrontierVcsFile:3:scpt:suites.FilemakerLib.openDocument

on openDocument (dbPath, reportError = false) {
	on doIt () {
		filemaker.openDocument (dbPath)};
	if reportError {
		return (doIt ())}
	else {
		try {
			return (doIt ())}
		else {
			return (false)}}}


