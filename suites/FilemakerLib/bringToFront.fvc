FrontierVcsFile:3:scpt:suites.FilemakerLib.bringToFront

on bringToFront (reportError = false) {
	on doIt () {
		filemaker.bringToFront ()};
	if reportError {
		return (doIt ())}
	else {
		try {
			return (doIt ())}
		else {
			return (false)}}}


