FrontierVcsFile:3:scpt:suites.navigator.setTarget

on setTarget (name) {
	target.set (cardOutline);
	op.firstsummit ();
	loop {
		local (linetext = op.getlinetext ());
		if string.nthfield (linetext, ';', 1) == name {
			return (true)};
		if not op.go (down, 1) {
			return (false)}}}


